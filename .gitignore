# OS / editor cruft
.DS_Store
Thumbs.db

# Cursor: track shared project rules, ignore everything else
# (MCP configs and local state may contain tokens or machine-specific paths)
.cursor/*
!.cursor/rules/
